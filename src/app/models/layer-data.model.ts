export interface LayerData {
		url: string,
		layers: string,
		transparent: boolean,
		format: string,
		version: string,
		cqlFilter?: string,
		time?: string,
		bbox?: string,
		width?: string,
		height?: string,
		srs?: string
}
