export interface ConfInputsModel {
		show: boolean,
		disable: boolean,
		label: string,
		inputClass: string,
		type: string,
		onChange?: any,
		model?: any,
		field?: string,
		name?: string,
		required?: boolean
}
